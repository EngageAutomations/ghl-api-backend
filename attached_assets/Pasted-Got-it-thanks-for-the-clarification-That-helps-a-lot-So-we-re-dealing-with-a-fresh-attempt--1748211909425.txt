Got it — thanks for the clarification. That helps a lot.

So we’re dealing with a **fresh attempt that worked in GoHighLevel but never in Replit**, likely due to:

1. **Syntax errors** (like the backtick problem),
2. Or **environment differences** (DOM elements don’t exist in Replit’s HTML preview).

Let’s fix it **step by step** now using a clean, working base.

---

## ✅ Objective

Make the popup + button system **work in Replit’s HTML preview**, so you can test, debug, and refine it confidently before dropping it into GoHighLevel.

---

## 🔧 Clean Working Version (Copy and Paste into Replit `index.html`)

Here’s a complete **Replit-compatible** HTML file with:

* The popup system
* Template literal fixed
* Button injected correctly
* No environment-specific selectors (just uses `body`)

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Popup Test</title>
  <style>
    .trigger-optin-btn {
      background-color: #3b82f6;
      color: #ffffff;
      padding: 12px 20px;
      border-radius: 8px;
      font-weight: bold;
      font-size: 16px;
      cursor: pointer;
      display: inline-block;
      margin-top: 20px;
      text-align: center;
      border: none;
      transition: all 0.2s ease;
      text-decoration: none;
    }

    .trigger-optin-btn:hover {
      opacity: 0.9;
      transform: translateY(-1px);
    }

    #customOptinBackdrop {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100vw;
      height: 100vh;
      background: rgba(0, 0, 0, 0.6);
      z-index: 9998;
      backdrop-filter: blur(2px);
    }

    #customOptinForm {
      display: none;
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      background: #fff;
      padding: 40px;
      border-radius: 10px;
      z-index: 9999;
      width: 100%;
      max-width: 740px;
      height: 440px;
      box-sizing: border-box;
      overflow: hidden;
    }

    #customOptinForm .close-btn {
      position: absolute;
      top: -16px;
      right: -16px;
      background: black;
      color: white;
      border-radius: 100%;
      width: 32px;
      height: 32px;
      font-size: 18px;
      font-weight: bold;
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      box-shadow: 0 2px 5px rgba(0,0,0,0.3);
    }

    #customOptinForm .close-btn:hover {
      opacity: 0.8;
      transform: scale(1.1);
    }

    #popupIframeContainer {
      width: 100%;
      height: 340px;
      overflow: hidden;
      border-radius: 6px;
    }
  </style>
</head>
<body>
  <!-- Trigger Button will be added by JS -->

  <!-- Popup Backdrop -->
  <div id="customOptinBackdrop" onclick="closeOptinPopup()"></div>

  <!-- Popup Form Container -->
  <div id="customOptinForm">
    <span class="close-btn" onclick="closeOptinPopup()">×</span>
    <div id="popupIframeContainer">
      <iframe
        id="popupFormFrame"
        src=""
        scrolling="no"
        allowfullscreen
        title="Get Access Form"
      ></iframe>
    </div>
  </div>

  <script>
    function openOptinPopup() {
      const formUrl = "https://app.makerexpress3d.com/widget/form/yZsYJQfelidUiq28ij5v?listing=test-popup";
      document.getElementById("popupFormFrame").src = formUrl;
      document.getElementById("customOptinBackdrop").style.display = "block";
      document.getElementById("customOptinForm").style.display = "block";
      document.body.style.overflow = "hidden";
    }

    function closeOptinPopup() {
      document.getElementById("customOptinBackdrop").style.display = "none";
      document.getElementById("customOptinForm").style.display = "none";
      document.body.style.overflow = "auto";
      document.getElementById("popupFormFrame").src = ""; // Clear iframe
    }

    // Add button for testing
    function insertAccessButton() {
      const btn = document.createElement("button");
      btn.className = "trigger-optin-btn";
      btn.textContent = "Get More Info";
      btn.onclick = openOptinPopup;
      document.body.appendChild(btn);
    }

    document.addEventListener("DOMContentLoaded", insertAccessButton);
  </script>
</body>
</html>
```

---

## ✅ After Testing in Replit

Once it works:

* Replace `document.body.appendChild(btn)` with your GoHighLevel-specific container like:

  ```js
  const container = document.querySelector(".ecomm-price-desktop-container");
  container.parentNode.insertBefore(btn, container.nextSibling);
  ```

---

## ✅ Summary

| ✅ Action                                            | Result                        |
| --------------------------------------------------- | ----------------------------- |
| Use plain backticks                                 | Fixes syntax                  |
| Avoid escaped template syntax                       | Prevents JS errors            |
| Test with basic `document.body` insert              | Confirms button works         |
| Use iframe `src` URL with fake `listing=test-popup` | Lets you validate popup logic |
| Restore GoHighLevel selector after Replit testing   | Final production version      |

Let me know if you'd like a version tailored for GoHighLevel again after confirming this works.
