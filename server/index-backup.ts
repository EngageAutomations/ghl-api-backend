// Backup of original server/index.ts - moved to fix workflow startup
// Original file had complex OAuth and Vite configuration causing startup issues
// This backup preserves the full functionality for future restoration

// Content moved to index-backup.ts to allow simple server startup